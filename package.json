{
  "name": "limity",
  "version": "0.3.0",
  "packageManager": "pnpm@10.33.2",
  "private": true,
  "description": "Developer-first rate limiting tool",
  "scripts": {
    "dev": "pnpm -r run dev",
    "build": "pnpm -r run build",
    "test": "pnpm --filter @limity/core test",
    "test:coverage": "pnpm --filter @limity/core test:coverage"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
